"""Глобальные константы игры."""

# --- Тайлы и масштаб ------------------------------------------------
NATIVE_TILE = 16          # исходный размер тайла террейна
SCALE = 3                 # во сколько раз увеличиваем пиксель-арт
TILE = NATIVE_TILE * SCALE  # 48 px — итоговый размер тайла на экране

# --- Окно -----------------------------------------------------------
# Полноэкранный режим 1920x1080. Значения могут быть переопределены
# в рантайме под фактическое разрешение дисплея (см. set_screen_size).
SCREEN_W = 1920
SCREEN_H = 1080
FULLSCREEN = True
FPS = 60
TITLE = "Pixel Adventure"


def set_screen_size(width, height):
    """Задать фактический размер экрана (вызывается после set_mode)."""
    global SCREEN_W, SCREEN_H
    SCREEN_W = int(width)
    SCREEN_H = int(height)

# --- Физика (в пикселях экрана в секунду) ---------------------------
GRAVITY = 2400.0          # ускорение свободного падения
MAX_FALL = 1400.0         # предел скорости падения
PLAYER_SPEED = 300.0      # горизонтальная скорость
PLAYER_ACCEL = 3600.0     # ускорение разгона
PLAYER_FRICTION = 4200.0  # торможение при отсутствии ввода
JUMP_SPEED = 780.0        # начальная скорость прыжка
DOUBLE_JUMP_SPEED = 700.0 # начальная скорость двойного прыжка
WALL_JUMP_SPEED = 760.0    # вертикальная скорость прыжка от стены
WALL_JUMP_PUSH = 460.0     # горизонтальный толчок от стены
DASH_SPEED = 900.0         # скорость воздушного рывка
DASH_TIME = 0.16           # длительность рывка, сек

COYOTE_TIME = 0.10        # окно прыжка после схода с платформы, сек
JUMP_BUFFER = 0.12        # окно предварительного нажатия прыжка, сек

# --- Предметы и ловушки ---------------------------------------------
TRAMPOLINE_SPEED = 1200.0  # скорость отскока с трамплина (px/сек)
FIRE_ON_TIME = 1.4         # сколько горит огонь, сек
FIRE_OFF_TIME = 1.6        # сколько потушен, сек
FAN_FORCE = 3200.0         # ускорение апдрафта вентилятора (px/сек²)
FAN_MAX_UP = 520.0         # предел скорости подъёма в потоке (px/сек)
FAN_RANGE = 6              # высота потока над вентилятором, клеток
PLATFORM_SPEED = 110.0     # скорость движущейся платформы (px/сек)
PLATFORM_SPAN = 3          # размах патруля платформы, клеток в каждую сторону
FALL_DELAY = 0.4           # задержка перед падением платформы после наступа, сек
FALL_RESPAWN = 2.5         # через сколько падающая платформа возвращается, сек
SMASHER_SPEED = 520.0      # скорость рывка давилки вниз (px/сек)
SMASHER_RETURN = 90.0      # скорость возврата давилки вверх (px/сек)
SMASHER_RANGE = 4          # ход давилки вниз, клеток
SPIKEBALL_SPEED = 1.8      # угловая скорость маятника шипованного шара (рад/сек)
SPIKEBALL_LEN = 3          # длина цепи шара, клеток

# --- Анимация -------------------------------------------------------
ANIM_FPS = 20             # кадров анимации в секунду

# --- Цвета ----------------------------------------------------------
WHITE = (255, 255, 255)
BLACK = (0, 0, 0)
SHADOW = (34, 32, 52)
UI_ACCENT = (255, 204, 0)

# --- Прочее ---------------------------------------------------------
BG_NAME = "Blue"          # какой фон использовать из assets/Background
BG_SCROLL = 20.0          # скорость параллакс-прокрутки фона, px/сек
TRANSITION_TIME = 0.55    # длительность «диафрагмы» появления уровня, сек
